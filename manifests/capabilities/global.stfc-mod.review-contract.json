{
  "manifestVersion": "axf/v0",
  "id": "global.stfc-mod.review-contract",
  "summary": "Blocking agent review contract: gameplay seam scanner, diff checks, and pure tests",
  "provider": "stfc",
  "adapterType": "cli",
  "executionTarget": {
    "launcher": {
      "command": "pwsh",
      "args": [
        "-NoLogo",
        "-NoProfile",
        "-ExecutionPolicy",
        "Bypass",
        "-File"
      ]
    },
    "target": {
      "path": "scripts/axf/review-contract.ps1",
      "relativeTo": "workspace"
    }
  },
  "argsSchema": {
    "type": "object",
    "properties": {
      "tail": {
        "type": "integer",
        "description": "Output tail line count for the pure-tests step."
      },
      "skip-pure-tests": {
        "type": "boolean",
        "description": "Skip pure-tests for local scanner-only debugging. Do not use for final review validation."
      }
    },
    "additionalProperties": false
  },
  "outputModes": [
    "json"
  ],
  "sideEffects": "write",
  "scope": "global",
  "lifecycleState": "active",
  "defaults": {
    "tail": 80
  },
  "policies": [
    "require_workspace_binding"
  ],
  "owner": "module:stfc",
  "argMap": {
    "tail": "-Tail",
    "skip-pure-tests": "-SkipPureTests"
  },
  "warnings": [
    "Side effects: runs pure-tests, which writes xmake build/test artifacts.",
    "This is a blocking review contract. Do not use -skip-pure-tests for final validation."
  ],
  "details": [
    "Runs scripts/scan_gameplay_seams.py as a blocking gameplay seam tripwire.",
    "Runs git diff --check and git diff --cached --check.",
    "Runs AX pure-tests through .ax/ax.ps1.",
    "Agents should run this before reporting a code or hook-affecting branch as review-ready."
  ]
}

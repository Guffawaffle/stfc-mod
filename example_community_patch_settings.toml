#  +--------------------------------------------------------------+
#  |                                                              |
#  |     #***  #****  #****   #***         #   *             #    |
#  |    #        #    #      #        #    #* **   #**       #    |
#  |     #**     #    #***   #             # * *  #   *   #***    |
#  |        #    #    #      #        #    #   *  #   *  #   *    |
#  |    #***     #    #       #***         #   *   #**    #***    |
#  |                                                              |
#  +--------------------------------------------------------------+
#   Generated on 2026-05-01T18:38:25.826Z
#
#   This configuration file was generated by https://modconfig.pages.dev
#   and is provided as-is.  If there are any issues with the STFC Community Mod
#   the configuration or the configuration website tool, please consider reporting
#   these to:
#
#       Discord : https://discord.gg/PrpHgs7Vjs
#       GitHub  : https://github.com/netniV/stfc-mod
#
#   Today's configuration file is brought to you by the letter M and the number 9
#

#  <[=======================================]>
#
#       ****            **     **
#       *   *  *   *   *      *      ****
#       ****   *   *  ***    ***    **
#       *   *  *  **   *      *       **
#       ****    ****   *      *     ***
#
#  <[=======================================]>

[buffs]
# This is the option to always show out of dock power for ships when they are docked. Defaults to false
use_out_of_dock_power = true

#  <[==============================================]>
#
#        ****                  **     *
#       *       ***   ***     *             ****
#       *      *   *  *  *   ***      *    *   *
#       *      *   *  *  *    *       *     ****
#        ****   ***   *  *    *       *     ***
#
#  <[==============================================]>

[config]
# Ignore this section, developer stuff only
assets_url_override = ""

# Ignore this section, developer stuff only
settings_url = ""

[battle_log_decoder]
# Legacy alias for sidecar.sync.battlelog_enrichment. Prefer the [sidecar.sync]
# key below when enabling local Battle Workbench enrichment.
enabled = false
# Decoder detail knobs used when sidecar.sync.battlelog_enrichment is enabled.
emit_segments = true
# Legacy compatibility placeholder; normal local Workbench enrichment is
# controlled by sidecar.sync.battlelog_enrichment.
emit_feed = true

#  <[=====================================================]>
#
#        ****                 *                     *
#       *       ***   ***    ***    ***     ***     *
#       *      *   *  *  *    *     *   *  *   *    *
#       *      *   *  *  *    *     *      *   *    *
#        ****   ***   *  *    **    *       ***     *
#
#  <[=====================================================]>

[control]
# Double-tap window in ms for 1-8 ship select/locate. Lower = faster, higher = slower. Default 500
select_timer = 500

# Subgroup: Hotkeys
# -----------------

# If you don't want any hotkeys you can set this to false (disables both mod and scopely hotkeys)
hotkeys_enabled = true

# If you prefer to disable the extended hotkeys set this to false
hotkeys_extended = true

# If you have the Kir'Shara artifact, should the queue be enabled by default?
queue_enabled = true

# If you prefer to use Scopely's hotkeys set this to true
use_scopely_hotkeys = false

# Subgroup: Options
# -----------------

# Enable experimental features
enable_experimental = false

# Subgroup: Input
# -----------------

[input]

# Scopely shortcut initialization policy: off, native, or fallback
scopely_shortcuts = "off"

# Original ScreenManager frame policy: mod, fallthrough_unhandled, or fallthrough_all
original_frame_policy = "mod"

#  <[============================================================]>
#
#        ****                       *        *
#       *      ***     ***   ****   *              ***    ****
#       *  **  *   *      *  *   *  ****     *    *      **
#       *   *  *       ****  ****   *   *    *    *        **
#        ****  *       ****  *      *   *    *     ***   ***
#
#  <[============================================================]>

[graphics]

# Subgroup: Display
# -----------------

# Amount of time to spend between transitions, increase to slow transitions down
transition_time = 0.01

# This specifies the amount to scale the UI by, 1.0 being default
ui_scale = 0.6

# This specifies the amount to adjust the scale by, 0.05 being default
ui_scale_adjust = 0.05

# This specifies the amount to scale the UI Object Viewers by, 1.0 being default
ui_scale_viewer = 1.2

# Subgroup: Keyboard
# ------------------

# This controls the zoom speed when using Q/E in System View, defaults to 350
keyboard_zoom_speed = 350.0

# Controls the system pan momentum speed. Defaults to 0.4
system_pan_momentum = 0.4

# Controls how quickly the momentum falls off when letting go of the mouse. Lower = quicker stop. Defaults to 0.8
system_pan_momentum_falloff = 0.8

# Subgroup: Loader
# ----------------

# Set to true to use the loader_image for the loading screen
loader_enabled = true

# Path to the custom loading screen image (leave blank for embedded image)
loader_image = ""

# Set to true to use the loader image for the transition screen
loader_transition = true

# Subgroup: Options
# -----------------

# Allow the system cursor to be replaced? (On by default)
allow_cursor = true

# Allow borderless full screen mode, set to false to disable
borderless_fullscreen = true

# Allow free resizing, set to false to disable
free_resize = true

# To enable additional resolution options set this to true
show_all_resolutions = false

# After using a zoom preset it will be set as default zoom
use_presets_as_default = true

# Subgroup: Zoom
# --------------

# Set to 0.0 to disable custom zoom; enter system at specified zoom otherwise
default_system_zoom = 1750.0

# Zoom preset F1 for system view
system_zoom_preset_1 = 50.0

# Zoom preset F2 for system view
system_zoom_preset_2 = 500.0

# Zoom preset F3 for system view
system_zoom_preset_3 = 1250.0

# Zoom preset F4 for system view
system_zoom_preset_4 = 2750.0

# Zoom preset F5 for system view
system_zoom_preset_5 = 5000.0

# This controls the maximum zoom distance in System View. Default is 2500. Higher values increase label distance
zoom = 5000.0

#  <[=============================================]>
#
#       *   *         *    *               ***
#       **  *   ***  ***         *   ***   *
#       * * *  *   *  *    *    **  *   *  ****
#       *  **  *   *  *    *    *   *****    *
#       *   *   ***    **  *   *     ***   ****
#
#  <[=============================================]>

[notifications]
# Toast-backed notification toggles are still flat during the schema migration.
# New notification delivery channels are shown below as nested tables.

# Supported: Battle notifications (fully parsed with player/ship detail)
# Each type must be individually enabled
notifications_victory = true
notifications_defeat = true
notifications_partial_victory = true
notifications_station_victory = false
notifications_station_defeat = false
notifications_station_battle = false
notifications_incoming_attack_player = false
notifications_incoming_attack_hostile = false
notifications_fleet_battle = false
notifications_armada_battle_won = false
notifications_armada_battle_lost = false
notifications_assault_victory = false
notifications_assault_defeat = false

# Supported: Armada notifications (parsed with alliance/owner detail)
notifications_armada_created = true
notifications_armada_canceled = true

# Supported: Event notifications (best-effort text resolution)
# These may still show raw placeholders depending on the game's toast payload.
notifications_tournament = true
notifications_chained_event_scored = true

# Experimental — these use generic text resolution and may show raw placeholders
# Enable individual types at your own risk
notifications_standard = false
notifications_faction_warning = false
notifications_faction_level_up = false
notifications_faction_level_down = false
notifications_faction_discovered = false
notifications_armada_incoming_attack = false
notifications_diplomacy_updated = false
notifications_joined_takeover = false
notifications_competitor_joined_takeover = false
notifications_abandoned_territory = false
notifications_takeover_victory = false
notifications_takeover_defeat = false
notifications_treasury_progress = false
notifications_treasury_full = false
notifications_achievement = false
notifications_challenge_complete = false
notifications_challenge_failed = false
notifications_strike_hit = false
notifications_strike_defeat = false
notifications_warchest_progress = false
notifications_warchest_full = false
notifications_arena_time_left = false
notifications_fleet_preset_applied = false
notifications_surge_warmup_ended = false
notifications_surge_hostile_group_defeated = false
notifications_surge_time_left = false

[notifications.system]
# Master switch for OS-native system notifications.
enabled = false

[notifications.system.fleet]
# Fleet-bar derived OS notifications.
arrived_in_system = false
arrived_at_destination = false
started_mining = false
node_depleted = false
docked = false
repair_complete = false

[notifications.audio]
# Master switch for in-game audible notification cues.
enabled = false
default_sound = "default"

[notifications.events.fleet]
# Compact per-notification policy rows. These override the legacy split tables above when present.
# system controls desktop notifications; audio controls the generated cue; sound chooses the cue name.
arrived_in_system = { system = false, audio = false, sound = "arrival" }
arrived_at_destination = { system = false, audio = false, sound = "soft" }
started_mining = { system = false, audio = false, sound = "ping" }
node_depleted = { system = false, audio = false, sound = "warning" }
docked = { system = false, audio = false, sound = "soft" }
repair_complete = { system = false, audio = false, sound = "repair" }

[notifications.audio.fleet]
# Fleet-bar derived audio cues. These are independent of OS notifications.
arrived_in_system = false

#  <[=====================================================]>
#
#       ****           *            *
#       *   *   ***   ***     ***   *       ***    ****
#       ****       *   *     *      ****   * **   **
#       *       ****   *     *      *   *  *        **
#       *       ****   **     ***   *   *   ****  ***
#
#  <[=====================================================]>

[patches]

# Subgroup: Leave Well Alone
# --------------------------

# Enable the buff fixes
bufffixhooks = true
chatpatches = true
freeresizehooks = true
hotkeyhooks = true
improveresponsivenesshooks = true
miscpatches = true
objecttracker = true
panhooks = true
resolutionlistfix = true
syncpatches = true
tempcrashfixes = true
testpatches = true
toastbannerhooks = true
uiscalehooks = true
zoomhooks = true

#  <[=========================================================================================================================================]>
#
#       *   *          *     *                                      *          ****  *                     *                    *
#       *   *   ***   ***    *  *    ***   *   *   ****            *          *      *       ***   ***    ***     ***   *   *  ***     ****
#       *****  *   *   *     ***    * **    ****  **              *            ***   ****   *   *  *   *   *     *      *   *   *     **
#       *   *  *   *   *     *  *   *          *    **           *                *  *   *  *   *  *       *     *      *  **   *       **
#       *   *   ***    **    *   *   ****   ***   ***           *             ****   *   *   ***   *       **     ***    ****   **    ***
#
#  <[=========================================================================================================================================]>

[shortcuts]

# NOTE: Some STFC/Unity input paths may not deliver ALT-* chords reliably.
# If an ALT binding below does not fire, prefer canonical overrides under
# [input.bindings]; see README.md "Alt shortcut fallback" for Ctrl/Shift examples.

# Subgroup: Cargo & Locate
# ------------------------

# Whether to show armada cargo by default
toggle_cargo_armada = "ALT-5"

# Whether to show cargo by default
toggle_cargo_default = "ALT-1"

# Whether to show Hostile cargo by default
toggle_cargo_hostile = "ALT-4"

# Whether to show Player cargo by default
toggle_cargo_player = "ALT-2"

# Whether to show Station cargo by default
toggle_cargo_station = "ALT-3"

# Whether to allow locate when previews are displayed
toggle_preview_locate = "CTRL-R"

# Whether to allow recall when previews are displayed
toggle_preview_recall = "CTRL-T"

# Subgroup: Chat Tabs & Selection
# -------------------------------

# CTRL-2
select_chatalliance = "CTRL-2"

# CTRL-1
select_chatglobal = "CTRL-1"

# CTRL-3
select_chatprivate = "CTRL-3"

# CTRL-SPACE
select_current = "CTRL-SPACE"

# See select_timer
select_ship1 = "1"

# See select_timer
select_ship2 = "2"

# See select_timer
select_ship3 = "3"

# See select_timer
select_ship4 = "4"

# See select_timer
select_ship5 = "5"

# See select_timer
select_ship6 = "6"

# See select_timer
select_ship7 = "7"

# See select_timer
select_ship8 = "8"

# Subgroup: Experimental
# ----------------------

# ⚠️ EXPERIMENTAL: Moves down the screen
move_down = "S"

# ⚠️ EXPERIMENTAL: Moves left the screen
move_left = "A"

# ⚠️ EXPERIMENTAL: Moves right the screen
move_right = "D"

# ⚠️ EXPERIMENTAL: Moves up the screen
move_up = "W"

# ⚠️ EXPERIMENTAL: Shows the bookmark->lookup screen
show_lookup = "L"

# Subgroup: Hotkeys Master Switch & Logs
# --------------------------------------

# CTRL-SHIFT-F9
log_debug = "CTRL-SHIFT-F9"

# CTRL-SHIFT-F11
log_error = "CTRL-SHIFT-F11"

# CTRL-SHIFT-F8
log_info = "CTRL-SHIFT-F8"

# CTRL-SHIFT-F12
log_off = "CTRL-SHIFT-F12"

# CTRL-SHIFT-F7
log_trace = "CTRL-SHIFT-F7"

# CTRL-SHIFT-F10
log_warn = "CTRL-SHIFT-F10"

# F10
quit = "F10"

# CTRL-ALT-MINUS
# If ALT is unreliable, prefer [input.bindings].hotkeys_disable = "CTRL-SHIFT-F6".
set_hotkeys_disable = "CTRL-ALT-MINUS"

# CTRL-ALT-=
# If ALT is unreliable, prefer [input.bindings].hotkeys_enable = "CTRL-SHIFT-F5".
set_hotkeys_enabled = "CTRL-ALT-="

# Subgroup: Queue & Primary
# -------------------------

# SPACE or right-click (Unity MOUSE1; left-click is MOUSE0)
action_primary = "SPACE|MOUSE1"

# Requires Kir'Shara; SPACE or right-click (Unity MOUSE1; left-click is MOUSE0)
action_queue = "SPACE|MOUSE1"

# Does not cancel primary attack
action_queue_clear = "CTRL-C"

# TAB or Mouse4
action_secondary = "TAB|MOUSE4"

# V or Mouse2
action_view = "V|MOUSE2"

# Requires Kir'Shara artifact
toggle_queue = "CTRL-Q"

# Subgroup: Recall & Repair
# -------------------------

# R or Mouse3
action_recall = "R|MOUSE3"

# SPACE or right-click (Unity MOUSE1; left-click is MOUSE0)
action_recall_cancel = "SPACE|MOUSE1"

# R or Mouse3
action_repair = "R|MOUSE3"

# Subgroup: Screens
# -----------------

# Show Alliance
show_alliance = "ALT-'"

# Show Alliance Armada
show_alliance_armada = "CTRL-'"

# Show Alliance Help
show_alliance_help = "SHIFT-'"

# Show Artifacts
show_artifacts = "SHIFT-I"

# Show Away Team
show_awayteam = "T"

# Show Bookmarks
show_bookmarks = "B"

# Show Chat Full Screen
show_chat = "C"

# Show Chat Left
show_chatside1 = "ALT-C"

# Show Chat Right
show_chatside2 = "`"

# Go to Fleet Commander
show_commander = "O"

# Go to Daily Missions
show_daily = "Z"

# Go to Events
show_events = "SHIFT-E"

# Go to Exocomp
show_exocomp = "X"

# Go to Factions
show_factions = "F"

# Go to Galaxy
show_galaxy = "G"

# Go to Gifts
show_gifts = "/"

# Go to Inventory
show_inventory = "I"

# Go to Missions
show_missions = "M"

# Go to Officers
show_officers = "SHIFT-O"

# Go to Q Trials
show_qtrials = "SHIFT-Q"

# Go to Refinery
show_refinery = "SHIFT-F"

# Go to Research
show_research = "U"

# Go to Scrap Yard
show_scrapyard = "Y"

# Go to Settings
show_settings = "SHIFT-S"

# Go to Manage Ships
show_ships = "N"

# Station Exterior
show_stationexterior = "SHIFT-G"

# Station Interior
show_stationinterior = "SHIFT-H"

# Go to System
show_system = "H"

# Subgroup: UI Scale
# ------------------

# PGDOWN
ui_scaledown = "PGDOWN"

# PGUP
ui_scaleup = "PGUP"

# SHIFT-PGDOWN
ui_scaleviewerdown = "SHIFT-PGDOWN"

# SHIFT-PGUP
ui_scaleviewerup = "SHIFT-PGUP"

# Subgroup: Zoom & Presets
# ------------------------

# CTRL-=
set_zoom_default = "CTRL-="

# SHIFT-F1
set_zoom_preset1 = "SHIFT-F1"

# SHIFT-F2
set_zoom_preset2 = "SHIFT-F2"

# SHIFT-F3
set_zoom_preset3 = "SHIFT-F3"

# SHIFT-F4
set_zoom_preset4 = "SHIFT-F4"

# SHIFT-F5
set_zoom_preset5 = "SHIFT-F5"

# Q
zoom_in = "Q"

# MINUS
zoom_max = "MINUS"

# BACKSPACE
zoom_min = "BACKSPACE"

# E
zoom_out = "E"

# F1
zoom_preset1 = "F1"

# F2
zoom_preset2 = "F2"

# F3
zoom_preset3 = "F3"

# F4
zoom_preset4 = "F4"

# F5
zoom_preset5 = "F5"

# =
zoom_reset = "="

#  <[==================]>
#
#       *   *    *
#       *   *
#       *   *    *
#       *   *    *
#        ***     *
#
#  <[==================]>
[ui]
# Skip the reveal box animation, and just open the boxes
always_skip_reveal_sequence = true

# Prevent "escape" from prompting to exit the game
disable_escape_exit = true

# When disable_escape_exit is true, this lets you double-tap Escape to still
# get the exit prompt. The value is the maximum gap in milliseconds between
# two Escape presses to count as a double-tap.
#
#   0   = double-tap disabled, Escape is fully blocked (default)
#   500 = press Escape twice within half a second to trigger the exit prompt
#
# Has no effect when disable_escape_exit is false.
escape_exit_timer = 0

# ⚠️ EXPERIMENTAL: Prevent the first popup advert from appearing
disable_first_popup = false

# ⚠️ EXPERIMENTAL: Prevent the standard move keys from working
disable_move_keys = false

# ⚠️ EXPERIMENTAL: Prevent the standard toast banners from working
disable_toast_banners = false

# Subgroup: Banners
# -----------------

# Specifies types of banners you don't want to see (comma separated)
disabled_banner_types = ""

# OS notification selection now lives under [notifications].
# Legacy [ui].notify_on_banner_types / notify_banner_types keys are still
# accepted for migration from older configs, but they are not documented here.

# Subgroup: Chat
# --------------

# Change to true to remove Galaxy Chat from the game
disable_galaxy_chat = false

# Change to true to remove Veil Chat from the game
disable_veil_chat = false

# Subgroup: Donations
# -------------------

# Extend donation slider to max value for next Alliance level
extend_donation_max = 80

# Extend donation slider enable
extend_donation_slider = true

# Subgroup: Previews
# ------------------

# Stop locate working when ship or node previews are displayed
disable_preview_locate = false

# Stop recall working when ship or node previews are displayed
disable_preview_recall = false

# Set to true to always show the rewards for selected ships/stations
show_armada_cargo = true

# Set to true to always show the rewards for selected ships/stations
show_cargo_default = true

# Set to true to always show the rewards for selected ships/stations
show_hostile_cargo = true

# Set to true to always show the rewards for selected ships/stations
show_player_cargo = true

# Set to true to always show the rewards for selected ships/stations
show_station_cargo = true

# Subgroup: Unsupported
# ---------------------

# This setting is not used
auto_confirm_discovery = true

#  <[===================================================================]>
#
#       ****           *                    ****
#       *   *   ***   ***     ***          *      *   *  ***     ***
#       *   *      *   *         *          ***    ****  *  *   *
#       *   *   ****   *      ****             *      *  *  *   *
#       ****    ****   **     ****         ****    ***   *  *    ***
#
#  <[===================================================================]>

[sync]
# The url of the server to proxy the connection through
proxy = ""

# The token that is used to provide authentication
token = ""

# The url of the server to send data to
url = ""

# Subgroup: Data
# --------------

battlelogs = true
buffs = true
buildings = true
fleet_runtime = false
inventory = true
jobs = true
missions = true
officer = true
research = true
resources = true
ships = true
slots = true
tech = true
traits = true

# Subgroup: Diagnostics
# ---------------------

debug = false
logging = false
# Sidecar-only local logging moved to [sidecar.logging].

# Subgroup: Network
# -----------------

resolver_cache_ttl = "300"
verify_ssl = true
# Leaving TLS verification enabled is strongly recommended. If verify_ssl is set
# to false, the mod ignores it unless this deliberately unsafe override is
# also true. When enabled, the mod warns loudly at launch because sync traffic
# can be intercepted by a proxy or network attacker.
allow_unsafe_tls_without_certificate_validation = false

#  <[======================================================================]>
#
#        ****   *      *          *
#       *      * *    ***      ***   ***   * **
#        ***   *****   *      *   *  *  *  **
#           *  *   *   *      *   *  *      *
#       ****   *   *   **      ***   *      *
#
#  <[======================================================================]>

[sidecar.sync]
# Dedicated local STFC Mod Sidecar delivery.
# This is the only supported home for sidecar-local HTTP config.
# Local Battle Workbench reads the sidecar event store through /api/events.
# Cloud Sync Monitor and Majel/cloud targets are not required for local sidecar ingest.
enabled = false
token = ""
url = "http://127.0.0.1:43127/api/sidecar/ingest"
proxy = ""
verify_ssl = true
allow_unsafe_tls_without_certificate_validation = false
# Sends raw battle capture events to the local sidecar. With this alone, the
# Workbench may show sparse capture-only battle rows.
battlelogs_realtime = false
# Decodes local sidecar battle journals and emits battle.report,
# catalog.snapshot, and battle.analytics events for Names <-> IDs, catalog
# snapshots, and Prime CSV parity rows. Legacy [battle_log_decoder].enabled is
# accepted as an alias, but this is the canonical local Workbench gate.
battlelog_enrichment = false
fleet_runtime = false

[sidecar.logging]
# Explicit opt-in local JSONL evidence/import feed for offline review and diagnostics.
# Keep this disabled unless you specifically need local file capture.
jsonl = false
# Keep the local sidecar JSONL feed cyclic so replay does not grow unbounded.
jsonl_replay_seconds = 30
# Optional second cap on retained battle-log groups.
jsonl_recent_logs = 300

[advanced.diagnostics]
# Canonical native observability/probing namespace for non-sidecar diagnostics.
# Older [sidecar.probes] and [sidecar.diagnostics] inputs are still accepted as deprecated aliases.
# `debug` remains a dormant compatibility placeholder in this slice.
# `logging` enables verbose native diagnostics payload logging for probes that opt into it.
# These battle probe keys do not enable normal local Battle Workbench enrichment;
# use [sidecar.sync].battlelog_enrichment for that producer path.
ship_identity = false
# Experimental disabled-by-default battle-log decoder probe. When enabled with
# [sidecar.sync].battlelog_enrichment, emits bounded runtimeAbilityRowCandidates
# under experimental report/analytics payloads for marker grammar discovery.
battle_log_decoder = false
battle_catalog = false
# Experimental disabled-by-default hostile observation probe for sidecar-local
# catalog work. When enabled, this also records diagnostics-only
# system-view section transition audit breadcrumbs. When [sidecar.sync].enabled
# has working local transport, it emits observed.hostile events into the
# sidecar event store for /api/observed-hostiles. It does not route through
# public [sync] targets or enable any public battle/feed output.
hostile_observation = false
debug = false
logging = false

# Development-only live query channel for AX. When enabled, `ax debug-send`
# can send one JSON request into the running game and receive a JSON response.
live_query = false

# Realtime trace level for sidecar/AX diagnostics: off, summary, detailed, or verbose.
# Disabled is the normal-play default. Every level above off adds runtime
# overhead, and longer investigations can increase local log/storage churn.
# Prefer sidecar/spocks.club ingress for durable export instead of long-lived
# local capture.
runtime_trace = "off"

# Include trace instrumentation overhead as its own probe so profiler cost can
# be separated from mod and game function timing. This adds more diagnostic work
# in exchange for cleaner attribution.
runtime_trace_track_overhead = false

# Emit periodic summaries of hook-owned runtime work. If runtime_trace is left
# unset, this enables summary-level tracing/reporting. If runtime_trace is set
# explicitly, that level still controls the effective trace mode.
mod_impact_monitor = false

# Milliseconds between runtime trace summary reports. Lower values increase
# diagnostic log churn. Values are clamped by the mod.
runtime_trace_report_interval_ms = 5000

# Emit focused refinery lifecycle, selection, analytics, and purchase-result
# breadcrumbs to the legacy plain-text community_patch.log troubleshooting log.
# Keep disabled during normal play because it detours shared shop/showcase
# methods, adds runtime overhead, and is not the preferred export path.
refinery_diagnostics = false

[advanced.diagnostics.files]
# Optional root directory for the heavier native diagnostics files in this
# slice. Empty preserves the current per-platform locations. When set to a
# usable directory, the navhook trace and action queue probe outputs write
# there.
# Example override for moving high-churn diagnostics off the game directory:
# root = "D:/stfc-diagnostics/native-logs"
root = ""

# Total navhook trace files to keep, including the active file.
# Default keeps about 4 MB per active trace file so one investigation cycle
# does not immediately churn away the evidence.
navhook_trace_max_kb = 4096
navhook_trace_files = 3

# Total action queue probe files to keep, including the active file.
# Default keeps about 8 MB per active JSONL file because structured queue
# traces are denser and usually need more history than the navhook trace.
action_queue_probe_max_kb = 8192
action_queue_probe_files = 3

[advanced.diagnostics.kirshara_queue]
# Focused Kir'shara queue marker diagnostics. These only install selected
# marker detours when [advanced.kirshara_queue].enabled is also true.
enabled = false

# Dump current ActionQueueManager method metadata at boot.
dump_interesting_methods = false

# Individual queued-combat completion markers. Keep disabled during normal
# play; enable only the narrow marker needed for the current investigation.
on_strike_complete = false
remove_target_and_attack_next = false
check_to_clear_action_queue = false
is_target_valid = false
process_queue_deployed = false
process_queue_target = false
on_set_course_response = false
on_player_fleet_state_changed = false
on_fleet_state_change = false
on_fleets_disposed = false

[advanced.kirshara_queue]
# Focused Kir'shara queued-combat advancement repair. Leave disabled unless
# you are validating the off-screen queue-stall regression workaround.
enabled = false

# Experimental repair candidate for off-screen queued combat. It replays the
# target-bearing queue commit when native only emits the player fleet combat
# start event. Keep false unless testing Kir'shara queue advancement.
course_target_completion = false

[advanced.queue]
# Legacy queue-add experiment/dev-test namespace. These keys do not control
# Kir'shara queue progression repair.

# Master opt-in gate for the queue-add path experiments below. Missing or false
# keeps the queue-add overrides inactive.
queue_repair_enabled = false

# Use the widget's queue-click event handler directly instead of the generic
# button press path. Ignored unless queue_repair_enabled is true.
queue_add_direct_handler = false

# When true, queue-add presses also close open viewers via DidHideViewers().
# Ignored unless queue_repair_enabled is true. Keep false during isolation work.
queue_add_hide_viewers = false

#  <[=========================================================================================================================================]>
#
#        ****                                                    *                                 *                      *           *
#       *      *   *  ***     ***          ***     ***   *   *  ***     ****  ****    ***    ***   *  *    ****   ***     *    *   *  *
#        ***    ****  *  *   *             *  *   * **    * *    *     **     *   *  *   *  *      ***    **     *        *    *   *  ****
#           *      *  *  *   *             *  *   *       * *    *       **   ****   *   *  *      *  *     **   *        *    *  **  *   *
#       ****    ***   *  *    ***          *  *    ****  *   *   **    ***    *       ***    ***   *   *  ***     ***     *     ****  ****
#
#  <[=========================================================================================================================================]>

[sync.targets.nextspocksclub]
# Ucomment the variables below to set them

# The token that is used to provide authentication
# token = ""

# The url of the server to send data to
# url = ""

# Outbound contract for this target: legacy or majel.
# mode = "legacy"

# Optional direct Majel target. This sends one-way cloud-safe ingest envelopes
# with Authorization: Bearer <token>. The token must be a Majel ingest token,
# not a game/session credential. No callbacks or remote settings writes are
# enabled by this target.
#[sync.targets.majel]
# token = ""
# url = "https://majel.example.test/api/ingest/events"
# mode = "majel"
# battlelogs = false
# fleet_runtime = true

#  <[=============================================================================================================]>
#
#        ****                                                          *                      *           *
#       *      *   *  ***     ***           ****  ****    ***    ***   *  *    ****   ***     *    *   *  *
#        ***    ****  *  *   *             **     *   *  *   *  *      ***    **     *        *    *   *  ****
#           *      *  *  *   *               **   ****   *   *  *      *  *     **   *        *    *  **  *   *
#       ****    ***   *  *    ***          ***    *       ***    ***   *   *  ***     ***     *     ****  ****
#
#  <[=============================================================================================================]>

[sync.targets.spocksclub]
# Ucomment the variables below to set them

# [sync.targets.*] is reserved for non-sidecar external/community sync consumers.

# The token that is used to provide authentication
# token = ""

# The url of the server to send data to
# url = ""

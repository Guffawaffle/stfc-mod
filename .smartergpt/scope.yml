version: 1
target: main
sources:
  - query: "is:pr is:open"
selectors:
  include_labels:
    - "area:windows-launcher"
  exclude_labels:
    - "do-not-merge"
    - "work-in-progress"
defaults:
  strategy: merge-weave
  base: main
pin_commits: true
